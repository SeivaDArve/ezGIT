#!/bin/bash
# If we know every repo as a .git dir in its root, then, despite the path you are currently on, which repo does your current path belong to?
# This function is similar to 'basename' but it looks for .git and tells the correct name of git repository
