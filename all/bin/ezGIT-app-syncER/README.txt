# Like 'upk' uses github sync, this script is intended to replicate that for any other app that wanta the same

# About: File extension:  .ezg means "ezGIT" and it is a file that contains info to be used by ezGIT while syncing other repositories othere than itself
# About: runMe-syncME-AppName.ezg it is the file we put inside each repository in order for DRYa or ezGIT to read and sync the app.
